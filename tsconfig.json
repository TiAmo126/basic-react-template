{
  "compilerOptions": {
    /* Basic Options */
    "target": "es5" /* 编译成哪个版本的 es: 'ES3' (default), 'ES5', 'ES2015', 'ES2016', 'ES2017', 'ES2018', 'ES2019' or 'ESNEXT'. */,
    "module": "commonjs" /* 指定生成哪个模块系统代码: 'none', 'commonjs', 'amd', 'system', 'umd', 'es2015', or 'ESNext'. */,
    "jsx": "react-jsx" /* 指定 JSX 代码生成: 'preserve', 'react-native', or 'react'. */,

    /* Strict Type-Checking Options */
    "strict": true /* 启用所有严格类型检查选项. */,

    /* Additional Checks */
    "noUnusedLocals": true /* Report errors on unused locals. */,

    /* Module Resolution Options */
    "baseUrl": "./" /* 解析非绝对模块名称的基目录. */,
    "paths": {
      "@/*": ["./src/*"]
    } /* 将导入重新映射到相对于“baseUrl”的查找位置的条目. */,
    "esModuleInterop": true /* Enables emit interoperability between CommonJS and ES Modules via creation of namespace objects for all imports. Implies 'allowSyntheticDefaultImports'. */,

    /* Source Map Options */
    "resolveJsonModule": true,
    "skipLibCheck": true
  }
}
